{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}\\bin\\${relativeFileDirname}.exe",
            "args": [],
            "cwd": "${fileDirname}",
            "preLaunchTask": "C/C++: clang.exe build active file",
        }
    ]
}